[build]
  base = "client"
  command = "rm -f ../package.json && npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_CONFIG_WORKSPACES = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
